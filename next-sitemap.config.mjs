/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://fullydigitalagency.online',
  generateRobotsTxt: true, // Generates robots.txt automatically
  generateIndexSitemap: false, // Optional: single sitemap
  changefreq: 'monthly',
  priority: 0.7,
};

export default config;